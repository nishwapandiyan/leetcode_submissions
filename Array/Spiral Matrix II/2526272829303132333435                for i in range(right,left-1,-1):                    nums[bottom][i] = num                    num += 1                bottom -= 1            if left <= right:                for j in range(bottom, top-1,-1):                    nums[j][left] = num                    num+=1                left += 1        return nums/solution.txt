                for i in range(right,left-1,-1):
                    nums[bottom][i] = num
                    num += 1
                bottom -= 1
            if left <= right:
                for j in range(bottom, top-1,-1):
                    nums[j][left] = num
                    num+=1
                left += 1
        return nums                    

